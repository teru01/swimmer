/** Polling interval for resource detail auto-refresh (ms) */
export const RESOURCE_DETAIL_POLL_INTERVAL_MS = 5000;

/** Polling interval for cluster overview auto-refresh (ms) */
export const CLUSTER_OVERVIEW_REFRESH_INTERVAL_MS = 30000;

/** Duration of tab close animation (ms) */
export const TAB_CLOSE_ANIMATION_MS = 200;

/** Duration to show "Copied!" feedback after copy action (ms) */
export const COPY_FEEDBACK_DURATION_MS = 1500;

/** Debounce delay for search highlight updates (ms) */
export const SEARCH_HIGHLIGHT_DEBOUNCE_MS = 150;

/** Character threshold for collapsing long metadata values */
export const VALUE_COLLAPSE_THRESHOLD = 80;

/** Default height for favorites section in context pane (px) */
export const FAVORITES_DEFAULT_HEIGHT = 200;
